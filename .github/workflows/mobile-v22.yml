name: Mobile v22
on:
  push:
    branches: [main]
    paths:
      - 'fix_fixed_categories.py'
      - '.github/workflows/mobile-v22.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply cleanup
        run: python fix_fixed_categories.py
      - name: Validate inline JavaScript
        run: |
          python - <<'PY'
          from pathlib import Path
          s=Path('test/index.html').read_text(encoding='utf-8')
          start=s.index('<script>')+len('<script>')
          end=s.rindex('</script>')
          Path('/tmp/customer-v22.js').write_text(s[start:end],encoding='utf-8')
          PY
          node --check /tmp/customer-v22.js
      - name: Commit result
        run: |
          git config user.name 'KOL Deploy Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add test/index.html test/kol-core.css
          git diff --cached --quiet || git commit -m 'Mobile v22 fix categories permanently'
          git push
