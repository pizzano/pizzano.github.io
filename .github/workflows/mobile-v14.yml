name: Mobile v14
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/mobile-v14.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 mobile_v14_simplify.py
      - run: |
          git config user.name 'KOL Deploy Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add test/index.html test/kol-core.css
          git commit -m 'Simplify mobile checkout into three clear steps' || exit 0
          git push origin HEAD:main
